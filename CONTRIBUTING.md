# Contributing

## Principles
- `core/` is the source of truth.
- `api/` contains no business logic.
- All changes must include tests or justified exceptions.

## Local Dev
1. Create venv
2. Install deps
3. Run tests
4. Run app

Use the Makefile targets where possible.