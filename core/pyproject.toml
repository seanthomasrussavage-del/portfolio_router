[project]
name = "core"
version = "0.1.0"
description = "Source of truth: schemas, validation, business logic."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
