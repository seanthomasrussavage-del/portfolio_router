[project]
name = "api"
version = "0.1.0"
description = "Thin API wrapper over core."
requires-python = ">=3.11"
dependencies = ["fastapi>=0.110", "uvicorn>=0.27"]

[tool.pytest.ini_options]
testpaths = ["tests"]
